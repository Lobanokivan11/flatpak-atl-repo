# flatpak-atl-repo

## Note!!! app from my repo can't be installed to system you need install app to 1 user only

```
flatpak --user remote-add --if-not-exists atl-repo https://lobanokivan11.github.io/flatpak-atl-repo/atl-repo.flatpakrepo
flatpak --user install atl-repo io.gitlab.android_translation_layer.BaseApp
```
