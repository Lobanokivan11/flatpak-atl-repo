# flatpak-atl-repo

```
flatpak --system remote-add --if-not-exists atl-repo https://lobanokivan11.github.io/flatpak-atl-repo/atl-repo.flatpakrepo
flatpak --system install atl-repo com.lagradost.cloudstream3
```
